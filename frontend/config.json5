{
  // The port to listen on - MUST BE DIFFERENT from back end
  port: 3000,

  // The host to proxy back end requests to (no protocol, no trailing slash)
  proxyTo: 'pxls-backend:4567',

  // Displayed on each page
  title: "Steller's Pixels",
}
